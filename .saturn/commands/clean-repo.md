# clean-repo

- Keep runtime files, canonical docs, and required scripts only.
- Remove caches, logs, UI, stale docs, dead assets, and wrapper folders.
- Print every path before delete or move.
- Move uncertain items outside the repo.
