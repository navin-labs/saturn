# deploy

- Compile Python before restart.
- Restart `saturn-api` only after local validation passes.
- Check health, queue behavior, tool listing, and daily report dedup after restart.
- Do not expose secrets in logs or docs.
